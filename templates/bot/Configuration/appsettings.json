{
  "Bot": {
    "Transport": {
      "Mode": "{{transport}}",
      "Webhook": {
        "PublicUrl": "{{public-url}}"
      }
    }
  },
  "Storage": {
    "Provider": "{{store}}",
    "Redis": {
      "Connection": "{{redis}}"
    },
    "Ef": {
      "Provider": "{{ef-provider}}",
      "Connection": "{{ef-conn}}"
    },
    "File": {
      "Path": "data"
    }
  }
}
